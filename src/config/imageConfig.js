/**
 * LoveCrafted Image Configuration Constants
 * -----------------------------------------
 * Shared constants for safe image payload thresholds in URL encoding and publishing.
 */

export const MAX_SAFE_IMAGE_PAYLOAD_BYTES = 25000; // ~25KB safe threshold for URL parameters
